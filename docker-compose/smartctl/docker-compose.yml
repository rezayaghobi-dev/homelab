services:
  smartctl_exporter:
    image: ghcr.io/prometheus-community/smartctl_exporter:latest
    container_name: smartctl_exporter
    privileged: true
    volumes:
      - /dev:/dev
      - /run/udev:/run/udev:ro
    ports:
      - "9633:9633"
    restart: unless-stopped
