services:
  sentinel:
    image: scyto/sentinel:latest
    container_name: sentinel
    privileged: true
    volumes:
      - /dev:/dev
      - /run/udev:/run/udev:ro
      - ./report:/etc/hdsentinel
    restart: unless-stopped