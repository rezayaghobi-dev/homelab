services:
  sentinel:
    image: scyto/sentinel:latest
    container_name: sentinel
    privileged: true
    volumes:
      - /dev:/dev
      - /run/udev:/run/udev:ro
    ports:
      - "8090:8090"
    restart: unless-stopped

