http:
  routers:
    homepage:
      rule: "Host(`homepage.home.server`)"
      entryPoints:
        - websecure
      service: homepage
      tls: {}
  services:
    homepage:
      loadBalancer:
        servers:
          - url: "http://192.168.100.6:30080"
